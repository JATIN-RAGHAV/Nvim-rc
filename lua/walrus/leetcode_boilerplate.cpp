#include "bits/stdc++.h"
using namespace std;
ostream& operator<<(ostream& os,vector<int> v){
	os <<"[" << v[0];
	for(int i =1;i<v.size();i++)
		os << ',' << v[i];
	os << "]\n";
	return os;
}

int main(){

}
